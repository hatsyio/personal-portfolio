# pnpm configuration
# Save exact versions (no ^ or ~)
save-exact=true

# Ensure using pnpm (prevent accidental npm/yarn usage)
engine-strict=true

# Use pnpm workspaces if needed in the future
# link-workspace-packages=true

